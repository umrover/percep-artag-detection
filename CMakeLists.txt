cmake_minimum_required(VERSION 3.10)
project("squareDetection")
find_package( OpenCV REQUIRED )
add_executable(squareDetection squareDetection.cpp)
target_link_libraries( squareDetection ${OpenCV_LIBS} )
